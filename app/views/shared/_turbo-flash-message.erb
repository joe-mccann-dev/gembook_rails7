<% flash.each do |message_type, message| %>
    <%= 
    content_tag(
      :p, 
      message, 
      class: "notification #{message_type} message", 
      data: { controller: "flash-message", "flash-message-target": "message" }
      ) 
    %>
  <% end %>